---
phase: 01-opencode-adoption
plan: 07
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - tests/integration/test_ingestion_workflow.py
  - tests/unit/test_llm.py
  - pytest.ini
  - HACKING.md
  - README.md
autonomous: false
requirements: [OPC-07]
user_setup:
  - service: real-llm-endpoint
    why: "Execute gated validation suite against live model service"
    env_vars:
      - name: REAL_LLM_TESTS
        source: "Local shell or CI environment variable"
      - name: LLM_BASE_URL
        source: "Provider endpoint URL"
      - name: LLM_API_KEY
        source: "Provider API key secret"
      - name: LLM_MODEL
        source: "Provider model identifier"
must_haves:
  truths:
    - "A dedicated real-LLM test path exists and is opt-in"
    - "Default test runs remain deterministic and mock-based"
    - "Live endpoint validation has clear acceptance criteria and failure signals"
  artifacts:
    - path: "tests/integration/test_ingestion_workflow.py"
      provides: "Real-endpoint gated integration tests"
    - path: "pytest.ini"
      provides: "Markers or gating config for live-endpoint tests"
    - path: "HACKING.md"
      provides: "Real-test execution and troubleshooting instructions"
  key_links:
    - from: "pytest.ini"
      to: "tests/integration/test_ingestion_workflow.py"
      via: "marker gating"
      pattern: "real_llm"
---

<objective>
Establish a gated real-LLM validation suite that can be run manually or in controlled CI jobs without destabilizing default tests.

Purpose: prove behavior against live provider while preserving deterministic baseline test runs.
Output: opt-in real-endpoint tests, gating config, and runbook criteria.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opencode-adoption/01-opencode-adoption-06-SUMMARY.md
@tests/integration/test_ingestion_workflow.py
@tests/unit/test_llm.py
@pytest.ini
@HACKING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce opt-in real-endpoint test marker and guards</name>
  <files>pytest.ini, tests/integration/test_ingestion_workflow.py</files>
  <behavior>
    - Test 1: Real-endpoint tests are skipped unless explicit env gate is enabled.
    - Test 2: Default integration suite continues to pass using mock server path.
  </behavior>
  <action>Add a dedicated `real_llm` marker and environment guard (`REAL_LLM_TESTS=1`) for live-endpoint integration scenarios, including skip reasons that explain missing configuration.</action>
  <verify>
    <automated>pytest tests/integration/test_ingestion_workflow.py -m "not real_llm" -x</automated>
  </verify>
  <done>Live endpoint tests cannot run accidentally and default test behavior remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add live validation assertions and runbook criteria</name>
  <files>tests/integration/test_ingestion_workflow.py, HACKING.md, README.md</files>
  <behavior>
    - Test 1: Live test validates workflow completion, status, and non-zero token usage.
    - Test 2: Live test records actionable failure output for auth, model, and timeout errors.
  </behavior>
  <action>Add a real-endpoint integration test path with robust assertions and update documentation with command examples for local and CI-gated execution.</action>
  <verify>
    <automated>REAL_LLM_TESTS=1 pytest tests/integration/test_ingestion_workflow.py -m real_llm -x</automated>
  </verify>
  <done>Real endpoint validation is reproducible and has clear pass/fail interpretation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Opt-in live-endpoint test path with gating and documentation.</what-built>
  <how-to-verify>
1. Export real endpoint credentials (`LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL`).
2. Run `REAL_LLM_TESTS=1 pytest tests/integration/test_ingestion_workflow.py -m real_llm -x`.
3. Confirm test output shows completed workflow and expected status/tokens.
4. Run default suite (`pytest tests/integration/test_ingestion_workflow.py -m "not real_llm"`) to confirm no regression.
  </how-to-verify>
  <resume-signal>Type "approved" if live and default paths both behave as expected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/env gate -> test execution | Missing gate can accidentally run costly live tests |
| live endpoint -> test harness | Provider instability can produce flaky outcomes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | D | accidental live test execution | mitigate | Require explicit env gate and marker selection for real-endpoint tests. |
| T-01-14 | R | flaky live assertions | mitigate | Assert only stable invariants (completion/status/token presence), not exact output text. |
</threat_model>

<verification>
- `pytest tests/integration/test_ingestion_workflow.py -m "not real_llm" -x` passes.
- `REAL_LLM_TESTS=1 pytest tests/integration/test_ingestion_workflow.py -m real_llm -x` passes in configured environments.
</verification>

<success_criteria>
Project has a reliable, opt-in real-LLM validation path for local and CI usage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-adoption/01-opencode-adoption-07-SUMMARY.md`
</output>
