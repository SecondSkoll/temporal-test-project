---
phase: 01-opencode-adoption
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - .env.example
  - README.md
  - HACKING.md
  - charm/src/charm.py
  - src/main.py
autonomous: false
requirements: [OPC-05]
user_setup:
  - service: opencode
    why: "Runtime provider used for documentation generation"
    env_vars:
      - name: LLM_PROVIDER
        source: "Worker environment/charm config"
      - name: OPENCODE_BASE_URL
        source: "Opencode deployment endpoint"
      - name: OPENCODE_API_KEY
        source: "Opencode credential secret store"
must_haves:
  truths:
    - "Operators can configure and run opencode provider in dev and deployed environments"
    - "Charm and worker startup wiring support provider settings"
    - "Runbooks document migration and rollback behavior"
  artifacts:
    - path: "README.md"
      provides: "Operator-facing setup and migration guidance"
    - path: "HACKING.md"
      provides: "Developer setup and test instructions for opencode"
    - path: "charm/src/charm.py"
      provides: "Charm config/secret wiring for opencode settings"
  key_links:
    - from: "charm/src/charm.py"
      to: "src/config.py"
      via: "environment variable injection"
      pattern: "LLM_PROVIDER|OPENCODE"
---

<objective>
Complete operational rollout by wiring deployment/runtime configuration and publishing migration documentation.

Purpose: make opencode adoption runnable in both local and charm-managed environments with a clear rollback path.
Output: updated runtime config wiring, deployment docs, and operator verification steps.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opencode-adoption/01-opencode-adoption-04-SUMMARY.md
@README.md
@HACKING.md
@charm/src/charm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rollout configuration for worker and charm environments</name>
  <files>.env.example, charm/src/charm.py, src/main.py</files>
  <action>Add and validate opencode environment/config mapping in local `.env.example` and charm configuration handling (including secrets). Ensure worker logs selected provider at startup for operational observability without leaking secrets.</action>
  <verify>
    <automated>pytest tests/unit -x</automated>
  </verify>
  <done>Runtime configuration can select opencode provider in both local and charm deployments.</done>
</task>

<task type="auto">
  <name>Task 2: Publish migration and rollback runbook updates</name>
  <files>README.md, HACKING.md</files>
  <action>Document migration steps, required environment variables, validation commands, and rollback procedure to previous provider path. Include explicit smoke-test commands for local and integration verification.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py tests/integration/test_ingestion_workflow.py -x</automated>
  </verify>
  <done>Docs provide complete operator/developer instructions for migrate, verify, and rollback flows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Opencode runtime selection and documentation updates for local and charm paths.</what-built>
  <how-to-verify>
1. Set `LLM_PROVIDER=opencode` and required opencode credentials in local environment.
2. Run worker and trigger a workflow using `python tools/trigger_workflow.py --package-name smoke-opencode --version 0.1.0`.
3. Confirm workflow completes and generated documentation is produced with expected statuses.
4. Switch provider back to prior value and confirm rollback path still executes.
  </how-to-verify>
  <resume-signal>Type "approved" if rollout behavior is correct, or share failure details for remediation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config -> runtime | Incorrect env/secret values can break or misroute provider usage |
| documentation -> operations | Inaccurate runbooks can cause unsafe rollout or failed rollback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | I | secret handling in charm | mitigate | Keep API keys in secret storage and avoid logging secret material. |
| T-01-10 | D | rollout/rollback procedure | mitigate | Provide tested smoke commands and explicit rollback instructions in docs. |
</threat_model>

<verification>
- `pytest tests/unit -x` passes.
- `pytest tests/integration/test_ingestion_workflow.py -x` passes.
- Human verification checkpoint is approved.
</verification>

<success_criteria>
Opencode integration is operationally deployable, documented, and reversible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-adoption/01-opencode-adoption-05-SUMMARY.md`
</output>
