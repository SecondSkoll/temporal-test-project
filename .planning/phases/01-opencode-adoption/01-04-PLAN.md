---
phase: 01-opencode-adoption
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - tests/unit/test_llm.py
  - tests/integration/test_ingestion_workflow.py
  - tools/mock_openai_server.py
  - tests/unit/test_triage.py
autonomous: true
requirements: [OPC-04]
must_haves:
  truths:
    - "Test suite validates provider-level behavior instead of direct SDK implementation details"
    - "Failure and retry semantics are verified under provider abstraction"
    - "Local test tooling can simulate opencode response patterns"
  artifacts:
    - path: "tests/unit/test_llm.py"
      provides: "Provider contract tests"
    - path: "tests/integration/test_ingestion_workflow.py"
      provides: "End-to-end provider-backed workflow assertions"
    - path: "tools/mock_openai_server.py"
      provides: "Mock responses for opencode-compatible scenarios"
  key_links:
    - from: "tests/integration/test_ingestion_workflow.py"
      to: "tools/mock_openai_server.py"
      via: "mock response protocol"
      pattern: "needs_more_context|fallback"
---

<objective>
Harden testing so opencode adoption remains safe during future refactors and upgrades.

Purpose: convert tests from transport-coupled to behavior-coupled guarantees.
Output: reliable unit/integration harness and mock support aligned to provider contracts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opencode-adoption/01-opencode-adoption-03-SUMMARY.md
@tests/unit/test_llm.py
@tests/integration/test_ingestion_workflow.py
@tools/mock_openai_server.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebase unit tests on provider contract fixtures</name>
  <files>tests/unit/test_llm.py</files>
  <behavior>
    - Test 1: Unit tests pass with provider fixture/mocks and no AsyncOpenAI patch dependency.
    - Test 2: Token and status assertions remain strict across round transitions.
  </behavior>
  <action>Replace direct SDK mocking patterns with provider fixture factories and contract-level assertions; retain full coverage of fallback, insufficient context, and error propagation paths.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py -x</automated>
  </verify>
  <done>Unit tests validate behavior through provider interface only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align integration and mock tooling with opencode behaviors</name>
  <files>tests/integration/test_ingestion_workflow.py, tools/mock_openai_server.py</files>
  <behavior>
    - Test 1: Integration tests can exercise success and fallback on opencode-compatible responses.
    - Test 2: Mock server can return deterministic response shapes for both rounds.
  </behavior>
  <action>Update integration scaffolding and mock endpoint payloads so tests represent the opencode pathway and keep deterministic coverage for retry and manual-triage routing outcomes.</action>
  <verify>
    <automated>pytest tests/integration/test_ingestion_workflow.py -x</automated>
  </verify>
  <done>Integration suite validates opencode behavior and local mocking is deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests -> production assumptions | Weak mocks can hide real regressions |
| mock server -> parser logic | Mock payload drift can invalidate test confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | mock payload schema | mitigate | Encode response contracts in shared fixtures and fail tests on schema mismatch. |
| T-01-08 | R | regression visibility | mitigate | Keep status/token assertions explicit in both unit and integration tests. |
</threat_model>

<verification>
- `pytest tests/unit/test_llm.py -x` passes.
- `pytest tests/integration/test_ingestion_workflow.py -x` passes.
</verification>

<success_criteria>
Automated tests verify opencode-backed behavior and no longer depend on direct API client internals.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-adoption/01-opencode-adoption-04-SUMMARY.md`
</output>
