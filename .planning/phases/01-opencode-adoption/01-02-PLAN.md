---
phase: 01-opencode-adoption
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/activities/llm.py
  - src/config.py
  - requirements.txt
  - tests/unit/test_llm.py
autonomous: true
requirements: [OPC-02]
must_haves:
  truths:
    - "An opencode-backed provider can be selected and used for generation"
    - "Provider-specific configuration is explicit and validated"
    - "No direct API call code is required on the opencode path"
  artifacts:
    - path: "src/activities/llm.py"
      provides: "Opencode adapter implementation and provider factory"
    - path: "src/config.py"
      provides: "Provider selection and opencode config settings"
    - path: "requirements.txt"
      provides: "Pinned opencode dependency"
  key_links:
    - from: "src/config.py"
      to: "src/activities/llm.py"
      via: "provider selection"
      pattern: "LLM_PROVIDER|OPENCODE"
---

<objective>
Implement the opencode adapter and provider factory so the activity can execute through opencode instead of direct client calls.

Purpose: introduce concrete opencode integration while preserving the contract from Plan 01.
Output: configurable provider path with dependency wiring and unit-test coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opencode-adoption/01-opencode-adoption-01-SUMMARY.md
@src/config.py
@src/activities/llm.py
@requirements.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider/runtime configuration for opencode</name>
  <files>src/config.py, requirements.txt</files>
  <behavior>
    - Test 1: Setting provider to opencode selects the opencode adapter path.
    - Test 2: Missing required opencode settings fails fast with clear configuration errors.
  </behavior>
  <action>Add `LLM_PROVIDER` with default-safe behavior, add opencode-specific env vars (endpoint/runtime options as needed by selected SDK), and pin the opencode package in dependencies. Keep existing LLM_* variables for transitional compatibility.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py -k "config or provider"</automated>
  </verify>
  <done>Configuration supports deterministic provider selection and validates opencode prerequisites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement opencode adapter and provider factory in activity layer</name>
  <files>src/activities/llm.py, tests/unit/test_llm.py</files>
  <behavior>
    - Test 1: Opencode adapter returns provider response shape expected by activity.
    - Test 2: Adapter maps opencode errors into retry categories without leaking SDK internals.
  </behavior>
  <action>Introduce provider factory and opencode adapter implementation in `llm.py` using Plan 01 contracts. Keep round-1/round-2 orchestration in activity unchanged while replacing transport calls with adapter invocations.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py -x</automated>
  </verify>
  <done>Activity executes via opencode adapter path when configured, with passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env config -> provider factory | Misconfiguration can route to unsafe or broken provider path |
| opencode SDK -> activity | SDK exceptions and payloads are untrusted until normalized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | S | provider selection | mitigate | Restrict provider selection to allowlisted values and fail closed. |
| T-01-04 | D | opencode transport | mitigate | Map transient errors to retryable categories; cap retries via existing Temporal policy. |
</threat_model>

<verification>
- `pytest tests/unit/test_llm.py -x` passes.
- Dependency installation with `pip install -r requirements.txt` succeeds.
</verification>

<success_criteria>
Opencode can be selected as the active provider and run under existing activity control flow without direct API client calls.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-adoption/01-opencode-adoption-02-SUMMARY.md`
</output>
