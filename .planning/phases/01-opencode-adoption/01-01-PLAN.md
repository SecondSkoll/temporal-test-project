---
phase: 01-opencode-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/generation.py
  - src/config.py
  - src/activities/llm.py
  - tests/unit/test_llm.py
autonomous: true
requirements: [OPC-01]
must_haves:
  truths:
    - "LLM invocation behavior is represented behind an explicit provider contract"
    - "Call-site expectations (inputs, outputs, errors, token usage) are documented in code-level contracts"
    - "Migration can proceed without changing workflow-level models"
  artifacts:
    - path: "src/models/generation.py"
      provides: "Provider request/response protocol models or typed contracts"
    - path: "src/activities/llm.py"
      provides: "Contract consumption boundaries for generation logic"
    - path: "tests/unit/test_llm.py"
      provides: "Contract-level tests that can run against any provider"
  key_links:
    - from: "src/activities/llm.py"
      to: "src/models/generation.py"
      via: "Typed provider request/response usage"
      pattern: "Provider|Request|Response"
---

<objective>
Create a stable provider abstraction contract so opencode can be introduced without regressions in workflow-facing behavior.

Purpose: decouple business logic in the activity from direct API client details.
Output: typed contracts and migration guardrails used by all later plans.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/activities/llm.py
@src/models/generation.py
@tests/unit/test_llm.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define provider interface contracts and invariants</name>
  <files>src/models/generation.py</files>
  <behavior>
    - Test 1: Provider response model must preserve markdown content, status, token counters, and model name fields.
    - Test 2: Provider error mapping must support retryable and non-retryable distinctions used by Temporal retries.
  </behavior>
  <action>Create explicit typed contracts for generation provider interactions (request payload, response payload, error category enum) with existing GenerationResult compatibility preserved. Do not alter workflow return schema; add only provider-layer types and lightweight helpers.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py -k "generation or prompt or context"</automated>
  </verify>
  <done>Provider contract types exist and are consumable without importing direct API client classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add contract-focused unit tests and de-risking assertions</name>
  <files>tests/unit/test_llm.py</files>
  <behavior>
    - Test 1: Activity-level tests can execute via provider protocol mocks rather than direct AsyncOpenAI patching.
    - Test 2: Existing statuses (success, fallback_used, insufficient_context) remain unchanged.
  </behavior>
  <action>Introduce tests that lock in provider-facing expectations (round sequencing, cumulative token accounting, status transitions) and remove assumptions that the implementation must instantiate AsyncOpenAI directly.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py -x</automated>
  </verify>
  <done>Tests fail if implementation bypasses provider contract or changes status/token semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activity -> LLM provider | Untrusted model output and transport errors enter here |
| config -> provider selection | Runtime selection input can alter execution path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | provider response parsing | mitigate | Validate and normalize provider responses through typed contracts before use. |
| T-01-02 | D | retry classification | mitigate | Enforce explicit retryable/non-retryable error mapping in provider interface layer. |
</threat_model>

<verification>
- `pytest tests/unit/test_llm.py -x` passes.
- `pytest tests/unit -m unit` passes with no new direct-client coupling failures.
</verification>

<success_criteria>
Provider contract exists, is test-enforced, and enables replacing direct API client logic without workflow schema changes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-adoption/01-opencode-adoption-01-SUMMARY.md`
</output>
