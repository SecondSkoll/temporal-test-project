---
phase: 01-opencode-adoption
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - src/config.py
  - .env.example
  - src/main.py
  - tools/trigger_workflow.py
  - HACKING.md
autonomous: true
requirements: [OPC-06]
user_setup:
  - service: real-llm-endpoint
    why: "Run pipeline against a production-like LLM service"
    env_vars:
      - name: LLM_BASE_URL
        source: "Provider endpoint URL"
      - name: LLM_API_KEY
        source: "Provider API key secret"
      - name: LLM_MODEL
        source: "Provider model catalog"
must_haves:
  truths:
    - "Operator can explicitly select real endpoint configuration without relying on mock defaults"
    - "Unsafe defaulting to mock credentials is prevented in real-server mode"
    - "Runtime startup clearly reports endpoint mode without leaking secrets"
  artifacts:
    - path: "src/config.py"
      provides: "Validated real-server configuration path and guardrails"
    - path: ".env.example"
      provides: "Documented real-server variables and safe defaults"
    - path: "tools/trigger_workflow.py"
      provides: "Smoke-test-friendly options and visible workflow result output"
  key_links:
    - from: "src/config.py"
      to: "src/main.py"
      via: "startup configuration logging"
      pattern: "LLM_BASE_URL|LLM_MODEL"
---

<objective>
Add a first-class real LLM runtime profile so developers can switch from mock to real endpoint safely and deterministically.

Purpose: make real-server testing an intentional, validated mode rather than an ad hoc env override.
Output: config guardrails, startup visibility, and smoke-testable trigger flow.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opencode-adoption/01-opencode-adoption-05-SUMMARY.md
@src/config.py
@src/main.py
@tools/trigger_workflow.py
@.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit real-server configuration validation</name>
  <files>src/config.py, .env.example</files>
  <behavior>
    - Test 1: Real endpoint mode requires non-placeholder API key and non-localhost base URL.
    - Test 2: Mock mode keeps current local defaults for developer convenience.
  </behavior>
  <action>Introduce explicit configuration checks for real-server mode and update `.env.example` to include a documented real endpoint profile. Keep compatibility with existing local mock defaults while failing fast on unsafe real-mode misconfiguration.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py -k "config or provider" -x</automated>
  </verify>
  <done>Configuration cannot silently run real-mode with invalid placeholder values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Improve runtime observability and smoke-trigger UX for real mode</name>
  <files>src/main.py, tools/trigger_workflow.py, HACKING.md</files>
  <behavior>
    - Test 1: Worker startup logs endpoint mode and model, but never logs full API keys.
    - Test 2: Trigger command output is sufficient to validate success/fallback and token usage in manual smoke runs.
  </behavior>
  <action>Update worker startup logging and trigger workflow output to support real endpoint smoke validation, and document exact commands and expected outputs in HACKING.</action>
  <verify>
    <automated>pytest tests/unit -x</automated>
  </verify>
  <done>Developer can run a real-endpoint smoke test with clear pass/fail indicators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars -> runtime client | Misconfigured endpoint or key can cause failed or unintended external calls |
| logs -> operators | Sensitive tokens can leak if logging is not sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | I | startup logging | mitigate | Redact secrets and log only endpoint host/mode/model metadata. |
| T-01-12 | D | real-mode misconfiguration | mitigate | Add startup validation and fail-fast errors for placeholder/invalid values. |
</threat_model>

<verification>
- `pytest tests/unit -x` passes.
- Manual smoke run reaches completed workflow state against a non-mock endpoint.
</verification>

<success_criteria>
Real-server mode is explicitly configurable, validated, and operationally observable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-adoption/01-opencode-adoption-06-SUMMARY.md`
</output>
