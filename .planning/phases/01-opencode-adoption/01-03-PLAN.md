---
phase: 01-opencode-adoption
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/activities/llm.py
  - src/workflows/ingestion.py
  - src/main.py
  - src/models/generation.py
  - tests/integration/test_ingestion_workflow.py
autonomous: true
requirements: [OPC-03]
must_haves:
  truths:
    - "Ingestion workflow behavior remains unchanged from caller perspective after provider migration"
    - "Fallback round flow still executes correctly via provider abstraction"
    - "Failure routing for insufficient context still works"
  artifacts:
    - path: "src/activities/llm.py"
      provides: "Provider-based orchestration for both generation rounds"
    - path: "src/workflows/ingestion.py"
      provides: "Unchanged workflow contract with provider-agnostic activity invocation"
    - path: "tests/integration/test_ingestion_workflow.py"
      provides: "Integration checks for status continuity"
  key_links:
    - from: "src/workflows/ingestion.py"
      to: "src/activities/llm.py"
      via: "workflow.execute_activity(generate_documentation)"
      pattern: "generate_documentation"
---

<objective>
Migrate runtime orchestration to the provider abstraction with no externally visible workflow regressions.

Purpose: ensure opencode adoption is transparent to upstream workflow callers.
Output: migrated activity wiring and integration-verified behavior parity.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opencode-adoption/01-opencode-adoption-02-SUMMARY.md
@src/workflows/ingestion.py
@src/activities/llm.py
@tests/integration/test_ingestion_workflow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve two-round orchestration with provider calls</name>
  <files>src/activities/llm.py</files>
  <behavior>
    - Test 1: Round 1 success returns status success with usage accounting preserved.
    - Test 2: needs_more_context response triggers round 2 and status fallback_used on success.
    - Test 3: two insufficient responses produce status insufficient_context.
  </behavior>
  <action>Refactor `generate_documentation` internals to exclusively use provider abstraction (opencode path active), while preserving current state machine and usage accumulation semantics.</action>
  <verify>
    <automated>pytest tests/unit/test_llm.py -k "fallback or insufficient or success" -x</automated>
  </verify>
  <done>Direct transport-specific code paths are removed from orchestration logic and behavior remains equivalent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate ingestion workflow parity under provider-backed activity</name>
  <files>tests/integration/test_ingestion_workflow.py, src/workflows/ingestion.py</files>
  <behavior>
    - Test 1: Happy path still returns GenerationResult status success.
    - Test 2: Fallback path still returns fallback_used.
    - Test 3: Oversized/manual triage behavior is unchanged.
  </behavior>
  <action>Update integration tests to assert workflow-level parity regardless of provider implementation details and adjust mocks to target provider boundaries rather than direct SDK objects.</action>
  <verify>
    <automated>pytest tests/integration/test_ingestion_workflow.py -x</automated>
  </verify>
  <done>Workflow integration tests pass with opencode-backed generation activity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow -> activity | Untrusted provider outcomes influence workflow branch logic |
| activity -> manual triage route | Error classification drives operational routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | status transition logic | mitigate | Keep explicit finite-state branching for success/fallback/insufficient and assert in tests. |
| T-01-06 | R | error routing | mitigate | Log provider error class and route reason to preserve auditability in workflow history. |
</threat_model>

<verification>
- `pytest tests/unit/test_llm.py -x` passes.
- `pytest tests/integration/test_ingestion_workflow.py -x` passes.
</verification>

<success_criteria>
Provider migration is complete in runtime path, and ingestion workflow external behavior remains stable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-adoption/01-opencode-adoption-03-SUMMARY.md`
</output>
