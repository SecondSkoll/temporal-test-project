# Interview questions

Note that the AI agent in question is currently theoretical, and answers are provided based on current designs.

1. Typical tasks performed by an AI agent will be installation and usage of the product binaries, or potentially the agent may help to build other software.

2. The agent has baked in context for the operating system, and is part of its core logic. The package its interacting with will be provided through user or system context, depending on the action it is tasked with.

3. This AI agent is currently theoretical, and has not made any mistakes at this time. However, without a source of strong Ubuntu otiented infomration about packages that are built and distributed through Ubuntu related infrastructure, it may confuse information with external resources.

4. When our agent encounters an unfamiliar package, it examines metadata, the existing documentation source, and potentially both source code and external documentation. The resources generated by this solution should replace external documentation, and be the primary source of information.

5. The AI agent will be "baked in" to the Ubuntu operating system and will be updated the same way the operating system is - likely through snaps. This will cover operating instrictions, reference documentation, and more.

6. At this time there is no preference for data type in terms of consumption. Research indicates Markdown format to be effetive, and will more effectively serve potential secondary consumers.

7. This is currently unknown. 

8. Plans indicate HTTP queries for content will be ade to retrieve external information.

9. Specifics are currently unknown.

10. A new remote repository of information should contain an index of information, which can be used to find information on specific packages. The agent can query this index, and follow with queries for the indexed files.

11. A centralised git repository may serve, however it is optimal for us to serve this information through a more performant webserver as git interactions can be slow, and the documentation will be read far more frequently than it is written to. 

## Team assumptions

1. This assumption is correct.

2. This assumption is untested, but this team cannot alter other impactful aspects of the system.

3. This assumption is intested, but is likely correct.

4. This assumption is correct, as this is assessed through the lens of accessibility rather than performance.

5. This is untested, but is assumed to be correct.

## Additional information

Infrastructure exists that can be utilised to automate serving contextual infomration about packages that are produced by Canonical and served for an Ubuntu context. This existing infrastructure uses Temporal workflows, and it would be optimal to use the existing infrastructure to add additional workflows for the purpose of this project.

Existing infrastructure uses a Juju deployment with charmed Temporal clusters and agents, and any solutions should keep this in mind.

It is unclear as to whether it is optimal for temporal workflows to be responsible for the generation of documentation artefacts or if they should be orchestrators of an external AI endpoint that generates and serves the documentation back to the worker.

## Instructions

Given the information, produce a draft of a problem statement, and a rough outline of the approach that should be taken to address the problem statement.